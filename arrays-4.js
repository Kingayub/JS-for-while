const array = ['JS', 'CSS', 'HTML', 'REACT', 'C++', 'Pyton', 'TypeScript', 'Pasqual', 'zayndi', 'ramzan'];
// const result = [];
let i = 0;
while (i<array.length) {
    if(array[i].length>3) {
        console.log(array[i]);
    }
    i++;
}