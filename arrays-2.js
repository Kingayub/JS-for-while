const array = ['hello', 'world', 'ayub', 'baysangur', 'adam', 'mohmad', 'yusup', 'islam', 'zayndi', 'ramzan'];
for (let i = 0; i < array.length; i++) {
    console.log(array[i]);
}

let i = 0;
while (i < array.length) {
    console.log(array[i])
    i++;
}

// console.log(typeOf array[i]);